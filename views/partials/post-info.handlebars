    <div>
       <a href="/post/{{id}}">{{post.title}}</a>
    </div>
    <div>
        {{post.body}}
    </div>
    <div>
        <p>{{user.username}} at {{format_date created_at}}</p>
    </div>
</article>