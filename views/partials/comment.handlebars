<div>
    {{#each this}}
    <section>
        <div>
            {{user.name}} on {{format_date date_created}}
        </div>
        <div class="text">
            {{comment.body}}
        </div>
    </section>
    {{/each}}
</div>